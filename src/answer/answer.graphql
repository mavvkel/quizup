# ============ Output types ============ 
type Answer {
  id: ID!
  content: String!
  rank: Int
  isCorrect: Boolean
}

# ============ Input types ============ 
input CreateAnswerInput {
  content: String!
  rank: Int
  isCorrect: Boolean
}
